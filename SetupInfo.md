pip install imutils
pip install -U numpy

#On RAspberryPI I needed command first: "pip install --upgrade pip setuptools wheel"
#on the pi, command below took a long time - maybe more than an hour
pip install opencv-python-4.5.3.56
#pip install dropbox
pip install azure-cognitiveservices-vision-computervision

#on Pi, did this also: https://stackoverflow.com/questions/53347759/importerror-libcblas-so-3-cannot-open-shared-object-file-no-such-file-or-dire

pip3 install opencv-python 
sudo apt-get install libcblas-dev
sudo apt-get install libhdf5-dev
sudo apt-get install libhdf5-serial-dev
sudo apt-get install libatlas-base-dev
sudo apt-get install libjasper-dev 
sudo apt-get install libqtgui4 
sudo apt-get install libqt4-test